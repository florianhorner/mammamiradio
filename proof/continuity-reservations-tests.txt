# Proof: test suite — PR #825 (protected continuity reservations + render timings)
# Captured 2026-07-11T22:59:43Z on a149b122 (pending eviction-test fix commit)

## Full suite WITH coverage (exact CI quality-gate path), rewritten eviction test
4615 passed, 3 skipped, 21 deselected — total coverage 93%, all module floors held

## Root cause of prior CI quality flake
test_cache_eviction_protects_capacity_exempt_continuity_slot awaited asyncio.to_thread
for the (mocked) evict_cache_lru; on a thread-saturated CI runner the mock invocation was
delayed past the poll deadline. Rewritten to run the eviction inline and wait on an
asyncio.Event, removing the ThreadPoolExecutor dependency. Verified under a deliberately
saturated 2-worker pool (completes in 0.01s).
