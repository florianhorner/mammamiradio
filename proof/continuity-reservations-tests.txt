# Proof: test suite — PR #825 (protected continuity reservations + render timings)
# Captured 2026-07-11T23:12:41Z on bff0c1e3 (pending eviction-test refactor commit)

## Full suite WITH coverage (exact CI quality-gate path)
4615 passed, 3 skipped, 21 deselected — total coverage 93%, all module floors held
(mammamiradio.scheduling.producer 89%, floor 83%)

## CI quality flake resolution
test_cache_eviction_protects_capacity_exempt_continuity_slot raced a wall clock against
run_producer reaching its eviction branch (CI-only 'evict not called' / TimeoutError,
never reproducible locally). Extracted the protected-paths computation into a pure helper
(_cache_eviction_protected_paths) that the loop uses, and now assert it directly — no loop,
no timing, no thread pool. The loop's eviction call site stays covered by full-loop tests.
