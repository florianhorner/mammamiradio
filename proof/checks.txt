Verification for florianhorner/fix/media-player-entity-feature
(run locally in .venv / Python 3.11 against the branch merged with origin/main)
Code commit: 7790e277 (fix(ha): add supported_features=0 to media_player state push)

$ python -m pytest tests/ -q
2340 passed, 2 skipped, 7 deselected, 1 warning in 69.75s

$ python -m pytest tests/home/test_ha_context.py -q
141 passed in 0.21s

$ ruff check .
All checks passed!

$ mypy mammamiradio/ tests/
Success: no issues found in 148 source files

$ bash scripts/check-release-invariants.sh
Passed: 6  Failed: 0 — All invariants passed.

$ bash scripts/check-changelog-lint.sh
Changelog lint clean. (no changelog touched — no release cut, single-line attribute fix)

Pre-ship review squad (3 parallel agents: adversarial / test-coverage / docs-config):
  P1 raised: "supported_features belongs at top-level, not in attributes dict"
  P1 verdict: FALSE POSITIVE — for REST API state pushes, all entity data (including
    supported_features) lives in the attributes dict. MA reads it via
    state.attributes.get("supported_features", 0). Top-level placement applies to
    native Python MediaPlayerEntity classes, not the /api/states REST push.
  Doc sync: docs/operations.md entity table updated to list supported_features.
  Test regression: assertion added to test_push_state_to_ha_uses_track_fallback_for_music.

New/changed tests:
  tests/home/test_ha_context.py::test_push_state_to_ha_uses_track_fallback_for_music
    (added: assert attributes["supported_features"] == 0)
