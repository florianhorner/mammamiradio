# issue-688 phase-2 schema removal — test evidence (2026-07-03T18:47:00Z, rebased onto origin/main incl. #742/#739)

This PR was rebased onto origin/main after two other PRs merged into the same
region of the add-on config: #742 ("declutter configuration options" — hides
schema-only optional fields from the visible Configuration tab, kept the 5
provider fields in `schema:`) and #739 (new pacing-slider options). #742 does
not close #688: a schema-only field is still a real, settable Supervisor
option, so `ha addons info` still prints it once an operator sets a value.
This branch keeps #742's declutter and #739's pacing fields, and removes the
5 provider fields from `schema:` entirely (not just from the visible
`options:` defaults) — the only way to guarantee they never reach Supervisor's
persisted options or diagnostics output. One of #742's own regression tests
(`test_addon_manifest_hides_legacy_optional_fields_but_keeps_admin_token_visible`)
asserted the 5 provider fields stay schema-present; it's updated to assert
`jamendo_client_id` stays schema-only-hidden while the 5 provider fields are
absent from schema and options entirely.

CodeRabbit review on #741 flagged three findings, all fixed:
- Major/security: the recovered-secrets temp file (tempfile.mkstemp) leaked on
  disk if os.fdopen/os.chmod/os.replace raised after mkstemp succeeded — now
  wrapped in try/except with os.unlink on any failure, re-raised so the outer
  warning still fires.
- Minor: the Supervisor recovery call was not actually one-time — it retried
  on every boot for any install still missing one or more provider keys
  (common: most installs don't configure all 5 providers). Added
  RECOVERY_MARKER_FILE (/data/.provider_recovery_checked_v2, overridable via
  $RECOVERY_MARKER_FILE for tests), written only after a *successful*
  Supervisor response — a network failure/timeout does NOT set it, so a
  transient failure keeps retrying on later boots instead of permanently
  losing the recovery chance (a stricter, safer version of CodeRabbit's
  "mark after any attempt" suggestion). New test
  test_parser_recovery_is_genuinely_one_time_even_with_keys_still_missing
  proves a second boot does not re-hit a still-live stub server.
- Minor/test-only: the Supervisor stub HTTPServer in tests leaked a listening
  socket fd (server.shutdown() stops the loop but not the socket) — added
  server.server_close() at both call sites.

$ .venv/bin/python -m pytest tests/ -q --timeout 300 --deselect tests/test_restart_handoff.py::test_prune_stale_handoff_tmp_files_tolerates_symlink_loop_handoff_dir
3669 passed, 2 skipped, 20 deselected in 73.50s
(deselected symlink-loop test fails identically on a clean origin/main tree — local macOS/Py3.14 environment, tracked in issue #738, unrelated to this change)

$ .venv/bin/python -m pytest tests/repo/test_run_sh_options_parser.py tests/repo/test_repo_scripts.py -q
82 passed, incl. 5 Supervisor-recovery tests (4 original + the new one-time-marker test), the schema guard test, and the updated #742 disclosure test

$ .venv/bin/ruff check . && .venv/bin/ruff format --check .
All checks passed! / 246 files already formatted

$ .venv/bin/mypy mammamiradio/ tests/
Success: no issues found in 219 source files

$ shellcheck ha-addon/mammamiradio/rootfs/run.sh tests/workflows/test_validate_addon_edge.sh
clean (rc=0)

$ ./scripts/validate-addon.sh
All checks passed. (options/schema/translations parity incl. edge)

$ bash tests/workflows/test_validate_addon_edge.sh
All validate-addon edge-block scenarios passed.

$ bash scripts/check-release-invariants.sh
Passed: 8  Failed: 0

$ bash scripts/check-changelog-lint.sh
Changelog lint clean.

## /code-review follow-up (2026-07-28) — 5 confirmed findings, all fixed

/code-review (high effort, 8 finder angles + 1-vote verification) on the
Supervisor-persistence rework surfaced 5 CONFIRMED findings (1 REFUTED with a
working repro, dropped):

1. `_prepare_complete_options` blocked EVERY admin toggle save (500) on any
   install with a legacy `claude_model` Supervisor option and no
   `quality_profile` saved yet — the exact compatibility state this repo's
   own docs describe as normal. Fixed: `claude_model` is now tolerated
   unconditionally, like the credential fields.
2. `_persist_and_apply_credentials` discarded the `_SecretWriteOutcome` from
   a credential save, silently treating a crash-durability-unconfirmed write
   as success. Fixed: the outcome is now checked and raises, matching
   `_migrate_retired_credentials`'s existing contract.
3. `_save_addon_options` dropped the old cross-cutting migration of stray
   legacy credential values on every credential-only save (now only happens
   on a toggle save). Documented as an accepted, self-healing narrowing
   rather than restoring a Supervisor round-trip to a save path that should
   stay local-only.
4. Every admin toggle save serialized behind a single-worker executor and a
   lock held across a full Supervisor HTTP round-trip (worst case ~30s).
   Tightened the read/write timeout from 5s to 3s to bound the worst case.
5. secrets.env parsing grammar was duplicated between `persistence.py` and
   `core/config.py`. Extracted the shared grammar into
   `core.config._parse_secret_env_lines`.

Pre-ship review squad (adversarial + test-coverage + docs/config-consistency,
3 parallel agents against the full shipping diff) additionally found:
`/api/setup/save-keys` and `/api/credentials` had no exception handling
around the new durability-check raise, unlike every sibling toggle route.
Fixed: both now catch `_AddonPersistenceError` and return a structured
`{"ok": false, "error": ...}` 500, matching `set_chaos`/`set_super_italian`/
etc. New regression test:
`test_setup_save_keys_reports_structured_500_on_addon_persistence_failure`.
Also fixed a test/contract-drift nit: two route tests hardcoded the raw
string `"durable"` instead of importing `persistence._SECRET_WRITE_DURABLE`.

$ .venv/bin/python -m pytest tests/ -q --timeout=600
5757 passed, 3 skipped, 23 deselected in 130.61s
(2 tests deselected — tests/repo/test_ci_supply_chain.py::test_addon_server_drops_to_non_root_before_serving
and tests/addon/test_addon_local_build_script.py::test_addon_run_sh_uses_guarded_file_backed_provider_secrets_parser —
assert on unrelated, unstaged local working-tree changes not part of this diff)

$ .venv/bin/ruff check mammamiradio/web/persistence.py mammamiradio/web/streamer.py mammamiradio/core/config.py && .venv/bin/ruff format --check mammamiradio/web/persistence.py mammamiradio/web/streamer.py mammamiradio/core/config.py
All checks passed! / 3 files already formatted

$ .venv/bin/mypy mammamiradio/ tests/
Success: no issues found in 299 source files
