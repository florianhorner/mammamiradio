configuration:
  station_name:
    name: Station Name
    description: Your radio station's name, shown in the UI and stream metadata.
  jamendo_client_id:
    name: Jamendo Client ID
    description: Enables CC-licensed music from Jamendo from the add-on Configuration tab. Get a free client ID at developer.jamendo.com. Leave empty to disable the Jamendo source.
  anthropic_api_key:
    name: Anthropic API Key
    description: Unlocks Claude AI hosts for generated banter and ads. Without this, the station can use OpenAI as a script fallback when configured, then stock banter.
  openai_api_key:
    name: OpenAI API Key
    description: Enables OpenAI host voices and serves as the script-generation fallback when Anthropic is unavailable. Without this, OpenAI hosts use Edge TTS and scripts fall back to stock copy.
  claude_model:
    name: AI Model
    description: "Haiku: fast and cheap. Sonnet: balanced. Opus: highest quality but slower."
  enable_home_assistant:
    name: Enable Home Assistant Integration
    description: "Let hosts use Home Assistant context, such as lights, presence, and climate, in banter and ads. Requires add-on API access; enable this if the boot log shows ha=disabled."
  admin_token:
    name: Admin Token
    description: "Set a stable token for the admin API and HA media_player integration. If left empty, one is auto-generated on each restart; check the add-on log to retrieve it."
  super_italian_mode:
    name: Super Italian Mode
    description: "On (default): listener UI and hosts are Italian-first. Off: listener UI uses English utility copy with Italian station-feel words intact, and AI hosts code-switch with Italian sprinkles."
  chaos_mode_active:
    name: Chaos Mode
    description: "Keeps host-chaos mode enabled across restarts. Turning it on from the admin panel still arms the immediate first strike; boot restore does not."
  festival_mode:
    name: Festival Mode
    description: "Turns your station into a theatrical music competition. Hosts announce songs as fictional Italian regional delegations, assign dramatic points, and call drinking game triggers. Toggleable live from the admin panel without a restart."
