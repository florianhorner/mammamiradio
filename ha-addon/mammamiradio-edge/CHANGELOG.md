# Changelog — Mamma Mi Radio (Edge)

The Edge add-on tracks `main`-branch builds. Its `version` is the `main` short commit
SHA, cut manually with `make edge-release` (no auto-bump) — there is no hand-written
release history here.

For what actually changed, see the root [`CHANGELOG.md`](../../CHANGELOG.md).
