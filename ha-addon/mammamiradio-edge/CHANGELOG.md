# Changelog — Mamma Mi Radio (Edge)

The Edge add-on always tracks the latest `main`-branch build. Its `version` is a
calendar version (`YYYY.M.D.<build>`) bumped automatically by CI on every
add-on-changing merge — there is no hand-written release history here.

For what actually changed, see the root [`CHANGELOG.md`](../../CHANGELOG.md).
