# Mamma Mi Radio (Edge)

The development channel. It is pinned to the newest tested `main` image
available when the maintainer cuts an Edge release, so it may trail `main`.

Use this one to try changes early and help catch problems. For the radio you
actually listen to, install **Mamma Mi Radio** instead — the two cannot run at
the same time, because they share port 8000.
